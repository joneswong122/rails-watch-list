<h1>My lists</h1>

<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list)%></li>
  <% end %>
</ul>

<%= link_to "Creat new list", new_list_path, class: "btn btn-primary mb3"%>
